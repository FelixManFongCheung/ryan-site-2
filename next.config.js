const path = require("path");

module.exports = {
  sassOptions: {
    includePaths: [path.join(__dirname, "styles")],
  },
  env: {
    Title: "Ryanmoyii",
    Intro: "b. 1998, Hong Kong Lives and works in Manchester and Leeds",
  },
};
